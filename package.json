{
  "name": "@mivir/react-discord-message",
  "version": "0.0.1",
  "main": "dist/index.js",
  "types": "dist/types.d.ts",
  "type": "module",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "build-docs": "rm -rf docs/dist && esbuild docs/src/docs.tsx --bundle --minify --format=esm --outfile=docs/dist/docs.js",
    "prepublish": "npm run build",
    "npm-publish": "npm publish --access public"
  },
  "author": "Miikka (Dragory)",
  "license": "MIT",
  "devDependencies": {
    "@types/react": "^17.0.39",
    "@types/react-dom": "^17.0.13",
    "@types/styled-components": "^5.1.24",
    "esbuild": "^0.14.25",
    "typescript": "^4.5.5"
  },
  "peerDependencies": {
    "react": ">=16",
    "react-dom": ">=16"
  },
  "dependencies": {
    "discord-api-types": "^0.27.3",
    "discord-markdown": "^2.5.1",
    "styled-components": "^5"
  }
}
